# Rust
target/
**/*.rs.bk
*.pdb

# Foundry / Solidity
/contracts/out/
/contracts/cache/
/contracts/broadcast/
/contracts/lib/

# Benchmark output. Raw results under results/ ARE committed, with their
# metadata alongside (EXPERIMENTS.md section 6: "commit raw CSV, not just
# figures"). Figures are regenerable and are not.
/results/scratch/
/figures/*.png
/figures/*.pdf

# Python (plotting)
__pycache__/
*.py[cod]
.venv/
venv/

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp

# Course material at repo root — not ours to redistribute
# (scoped to root so docs/*.pdf deliverables still track)
/*.pdf

# Slide tooling
scripts/slides/node_modules/
scripts/slides/package*.json
scripts/slides/qa.json
scripts/slides/preview/
